apiVersion: v1
kind: Service
metadata:
  name: detoserve-agent
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: detoserve-agent
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: detoserve-agent
