apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-cluster-manager
  namespace: {{ .Values.global.namespace }}
  labels:
    app: cluster-manager
    {{- include "detoserve.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.clusterManager.replicaCount }}
  selector:
    matchLabels:
      app: cluster-manager
  template:
    metadata:
      labels:
        app: cluster-manager
    spec:
      containers:
        - name: cluster-manager
          image: "{{ .Values.clusterManager.image.repository }}:{{ .Values.clusterManager.image.tag }}"
          ports:
            - containerPort: {{ .Values.clusterManager.port }}
          env:
            - name: PORT
              value: {{ .Values.clusterManager.port | quote }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.clusterManager.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.clusterManager.port }}
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-cluster-manager
  namespace: {{ .Values.global.namespace }}
  labels:
    app: cluster-manager
    {{- include "detoserve.labels" . | nindent 4 }}
spec:
  selector:
    app: cluster-manager
  ports:
    - port: {{ .Values.clusterManager.port }}
      targetPort: {{ .Values.clusterManager.port }}
